# Magnetic-text
🧲 Magnetic Text — Interactive Cursor Effect  Magnetic Text is an interactive typography animation where each letter reacts individually to mouse movement. The text is split into separate &lt;span> elements so every character can move independently instead of acting as one block.
